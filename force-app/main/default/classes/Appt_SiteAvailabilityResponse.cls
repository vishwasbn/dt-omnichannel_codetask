//
// Generated by JSON2Apex http://json2apex.herokuapp.com/
//

global class Appt_SiteAvailabilityResponse {

	public String status;
	public String message;
	public List<Days> days;

	public class Days {
		public String scheduleDate;
		public String year;
		public String month;
		public String day;
		public String dayOfWeek;
		public List<TimeSlots> timeSlots;
	}

	public class TimeSlots {
		public String consultStartTime;
		public String consultEndTime;
		public String consultDuration;
		public String backOfficeStartTime;
		public String backOfficeEndTime;
		public String backOfficeDuration;
	}

	
	public static Appt_SiteAvailabilityResponse parse(String json) {
		return (Appt_SiteAvailabilityResponse) System.JSON.deserialize(json, Appt_SiteAvailabilityResponse.class);
	}

	public Appt_SiteAvailabilityResponse() {}

	public Appt_SiteAvailabilityResponse(Appt_SiteAvailabilityModel model) {
		this.status = model?.status;
		this.message = model?.message;
		this.days = new List<Days>();
		
		if (model != null && model.days != null) {
			for (Appt_SiteAvailabilityModel.Days day : model?.days) {
				Days newDay = new Days();
				newDay.scheduleDate = day?.scheduleDate;
				newDay.year = day?.year;
				newDay.month = day?.month;
				newDay.day = day?.day;
				newDay.dayOfWeek = day?.dayOfWeek;
				newDay.timeSlots = new List<TimeSlots>();
				
				for (Appt_SiteAvailabilityModel.TimeSlots slot : day?.timeSlots) {
					TimeSlots newSlot = new TimeSlots();
					newSlot.consultStartTime = slot?.consultStartTime;
					newSlot.consultEndTime = slot?.consultEndTime;
					newSlot.consultDuration = slot?.consultDuration;
					newSlot.backOfficeStartTime = slot?.backOfficeStartTime;
					newSlot.backOfficeEndTime = slot?.backOfficeEndTime;
					newSlot.backOfficeDuration = slot?.backOfficeDuration;
					newDay.timeSlots.add(newSlot);
				}
				this.days.add(newDay);
			}
		}
		
	}
}